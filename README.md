# XeonDB's website

## Install and update

To install XeonDB, run the following command in your terminal:
```bash
curl -L https://xeondb.com/install | bash
```

To update XeonDB, run the following command in your terminal:
```bash
curl -L https://xeondb.com/update | bash
```